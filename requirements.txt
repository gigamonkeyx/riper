# Core PyTorch and GPU acceleration
torch>=2.0.0  # EvoTorch backbone and CUDA support for RTX 3080
torchvision>=0.15.0  # Additional vision utilities if needed
torchaudio>=2.0.0  # Audio processing support for TTS integration

# Evolutionary Algorithm Libraries
evotorch>=0.4.0  # Evolutionary algorithms on PyTorch (docs.evotorch.ai)
deap>=1.3.3  # Genetic algorithm implementations (deap.readthedocs.io)

# Local Model Integration
ollama>=0.6.7  # Local model API integration with timeout fixes

# Camel-AI with pinned versions for conflict resolution
camel-ai==0.2.70
pydantic==2.11.7
tiktoken==0.7.0
pillow==10.4.0

# Dependency management
pipdeptree>=2.13.0

# OpenRouter Integration for Qwen3
requests>=2.28.0  # HTTP client for OpenRouter API calls
httpx>=0.24.0  # Alternative async HTTP client (optional)
python-dotenv>=1.0.0  # Environment variable management for API keys

# A2A Protocol Implementation
# a2a-py>=1.0.0  # A2A protocol implementation (a2aprotocol.ai/blog)
# Note: Install manually if not available via pip

# Neural Network and ML Utilities
numpy>=1.21.0  # Numerical computing foundation
scipy>=1.7.0  # Scientific computing for optimization algorithms
matplotlib>=3.5.0  # Plotting and visualization for fitness metrics
seaborn>=0.11.0  # Enhanced statistical plotting

# Multi-Agent Framework Support
# crewai>=0.1.0  # Multi-agent workflows (docs.crewai.com)
# Note: Install manually if needed for swarm simulations

# Audio Processing (TTS Integration)
# bark>=1.0.0  # Bark TTS transformer (suno-ai/bark GitHub)
# Note: Install manually for TTS chaining with Ollama

# Development and Testing
pytest>=7.0.0  # Testing framework for evo algorithms
pytest-cov>=4.0.0  # Coverage reporting
black>=22.0.0  # Code formatting
flake8>=5.0.0  # Linting

# Utilities
pyyaml>=6.0  # Configuration file parsing
requests>=2.28.0  # HTTP requests for API integration
tqdm>=4.64.0  # Progress bars for evolutionary loops
psutil>=5.9.0  # System monitoring for GPU utilization

# Optional: Enhanced GPU Monitoring
# nvidia-ml-py3>=7.352.0  # NVIDIA GPU monitoring
# gpustat>=1.0.0  # GPU status monitoring

# Development Notes:
# - Prioritize pre-installed packages for offline operation
# - RTX 3080 compatibility verified for torch>=2.0.0
# - Local GPU focus: no cloud-dependent packages in runtime
# - Manual installation required for: a2a-py, crewai, bark
# - GPU memory optimization: <10GB VRAM target
# - Performance target: 7-15 tok/sec on RTX 3080
